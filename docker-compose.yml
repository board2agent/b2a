services:
  board:
    build: ./board
    ports:
      - "3000:3000"
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_OWNER=${GITHUB_OWNER}
      - GITHUB_REPO=${GITHUB_REPO}
      - POLL_INTERVAL_MS=5000

  hello:
    build: ./hello
    ports:
      - "8080:8080"
